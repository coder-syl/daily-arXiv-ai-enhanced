<div id=toc></div>

# Table of Contents

- [cs.CV](#cs.CV) [Total: 1]


<div id='cs.CV'></div>

# cs.CV [[Back]](#toc)

### [1] [From Weights to Concepts: Data-Free Interpretability of CLIP via Singular Vector Decomposition](https://arxiv.org/abs/2603.24653)
*Francesco Gentile,Nicola Dall'Asen,Francesco Tonini,Massimiliano Mancini,Lorenzo Vaquero,Elisa Ricci*

Main category: cs.CV

TL;DR: 本文提出SITH，一种无需数据、无需训练的框架，通过在权重空间直接分析CLIP视觉Transformer的注意力头，实现对其内部机制的细粒度、可解释性理解，并支持精准的概念级模型编辑。


<details>
  <summary>Details</summary>
Motivation: 现有视觉-语言模型的可解释性方法主要依赖激活值，存在数据依赖性强、易受数据偏差影响以及解释粒度粗糙（仅限于注意力头级别）等局限性。为克服这些问题，需要一种更鲁棒、细粒度且与数据无关的可解释性方法。

Method: 作者提出了SITH（Semantic Inspection of Transformer Heads）框架，该框架完全不依赖数据和训练。它通过分解每个注意力头的值-输出矩阵的奇异向量，并利用一种名为COMP（Coherent Orthogonal Matching Pursuit）的新算法，将这些向量解释为人类可理解概念的稀疏且语义连贯的组合。

Result: SITH能够为每个注意力头提供连贯且忠实的内部解释，这一点通过重构保真度和可解释性实验得到了验证。利用该方法，可以在权重空间进行精确的模型编辑，以放大或抑制特定概念，从而在不重新训练的情况下提升下游任务性能。此外，SITH揭示了微调过程主要是对一个稳定的语义基进行重新加权，而非学习全新的特征。

Conclusion: SITH提供了一种强大的、数据无关的视角来剖析和理解视觉-语言模型的内部工作机制，不仅实现了高保真的可解释性，还为无需重训练的精准模型编辑和对模型适应机制的研究开辟了新途径。

Abstract: As vision-language models are deployed at scale, understanding their internal mechanisms becomes increasingly critical. Existing interpretability methods predominantly rely on activations, making them dataset-dependent, vulnerable to data bias, and often restricted to coarse head-level explanations. We introduce SITH (Semantic Inspection of Transformer Heads), a fully data-free, training-free framework that directly analyzes CLIP's vision transformer in weight space. For each attention head, we decompose its value-output matrix into singular vectors and interpret each one via COMP (Coherent Orthogonal Matching Pursuit), a new algorithm that explains them as sparse, semantically coherent combinations of human-interpretable concepts. We show that SITH yields coherent, faithful intra-head explanations, validated through reconstruction fidelity and interpretability experiments. This allows us to use SITH for precise, interpretable weight-space model edits that amplify or suppress specific concepts, improving downstream performance without retraining. Furthermore, we use SITH to study model adaptation, showing how fine-tuning primarily reweights a stable semantic basis rather than learning entirely new features.

Abstract (中文翻译): 随着视觉-语言模型的大规模部署，理解其内部机制变得愈发关键。现有的可解释性方法主要依赖于激活值，这使其具有数据依赖性、易受数据偏差影响，并且通常只能提供粗粒度的、基于注意力头层面的解释。我们提出了SITH（Transformer注意力头的语义检查），这是一个完全无需数据、无需训练的框架，可直接在权重空间中分析CLIP的视觉Transformer。对于每个注意力头，我们将其次的值-输出矩阵分解为奇异向量，并通过一种名为COMP（相干正交匹配追踪）的新算法对每个向量进行解释，将其表示为人类可理解概念的稀疏且语义连贯的组合。我们证明了SITH能够产生连贯且忠实的头内解释，这一点已通过重构保真度和可解释性实验得到验证。这使得我们可以利用SITH在权重空间中进行精确且可解释的模型编辑，以放大或抑制特定概念，从而在无需重新训练的情况下提升下游性能。此外，我们利用SITH研究了模型的适应过程，表明微调主要是对一个稳定的语义基进行重新加权，而非学习全新的特征。

</details>
